{
  "name": "hero-rush",
  "version": "1.0.0",
  "description": "Browser-based 3D arena squad game.",
  "private": true,
  "scripts": {
    "start": "node dev-server.js",
    "check": "node --check game.js && node --check dev-server.js && node --check check-3d.js",
    "test:3d": "node check-3d.js"
  },
  "devDependencies": {
    "playwright": "^1.45.0"
  }
}
